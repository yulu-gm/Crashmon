# Crashmon Agent 工作约定

## 阅读入口

先读 README.md 与 docs/README.md，再读取当前任务对应设计。底层任务看 docs/architecture/，玩法任务看 docs/game-design/，版本任务看 docs/versions/，具体内容看 docs/content/。

完整讨论快照是 docs/overview/design-v0.1.md；确认记录在 docs/decisions/README.md。快照包含建议和待定项，不得将其全部视为批准。

## 当前状态

当前已有注册、登录与基础档案的本地原型，含 HTTP 接口、网页和隔离集成测试。运行命令与实现边界见 docs/architecture/account-save-milestone.md。另有二维据点、NPC 交互与一次性初始领宠实现，见 docs/game-design/hub-prototype.md。战斗、野外、CI 与部署尚未实现；不得把原型测试通过当作正式运营验收。用户已授权账号与存档、二维据点与初始领宠实现，不代表授权部署或修改生产数据。

## 设计与实现状态

- 设计状态区分 Draft（草稿）和 Approved（人类确认），确认要记录来源和范围。
- 实现与验证另行记录。代码存在、测试执行和测试通过是不同状态。
- 不静默决定多人形式、技术栈、队伍规模、数值或资产处置；建议和临时假设必须标注。
- 草稿不能覆盖已确认规则。发现文档冲突时说明差异，不仅凭更新时间猜测。
- 原始总体方案保留为快照；日常修改主题正文，重要确认登记在 decisions。

## 边界与复杂度

普通内容任务默认只改当前内容、对应设计与测试。需要改公共规则、存档、权限、依赖体系或发布检查时，明确提出公共能力缺口并进入授权的底层维护任务。

常驻系统不主动导入具体活动或宠物，由 app 装配。内容只使用公开能力，不绕过资产结算，不依赖其他活动内部代码。永久宠物不能依赖获宠活动保持开放。

允许清晰的一次性业务代码，不为未知未来增加空类、工厂、微服务或通用脚本语言。新结构必须有当前用例依据。本轮账号与存档原型采用 Node.js 内置模块和 SQLite，无第三方依赖。未来游戏技术栈未确认前不安装相关依赖。

活动启停、内容切换和任意代码热替换需要分别设计与验收。目录和提示词不是运行时安全沙箱。

## 验证与交付

不得删除、弱化或跳过关键测试制造通过结果。交付说明真实改动、实际检查、未验证范围、存档影响以及需人类确认的决定。

文档阶段可以校验链接、目录与原文完整性，不能宣称游戏测试通过。未来代码验证需绑定实际候选提交，独立 Agent 的同意不能替代运行证据。

普通开发不使用生产存档、密钥或正式发奖能力，不擅自部署、改权限或恢复数据库。本文件是协作约定，不是已经配置的仓库保护和生产权限控制。
